module.exports = {
	secret: 'setapakbogorapimaster'
}

// var token = req.headers.authorization    
// 	//JWT VERIFY     
//         jwt.verify(token, secret, function(err, decoded) {
//         	if(err) {
//             return res.status(401).send({message: 'invalid_token'});
//         	}else{
//         	var user_id = decoded.user_id
//         	}
//         });